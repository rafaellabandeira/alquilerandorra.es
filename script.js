// Selector de idioma (placeholder, se puede ampliar)
document.getElementById('language').addEventListener('change', function() {
    const lang = this.value;
    alert('Cambiar idioma a: ' + lang + ' (funcionalidad aún no implementada)');
});

